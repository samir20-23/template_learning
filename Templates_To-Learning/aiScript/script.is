// Example of AiScript code

// Define a simple function
function greet(name) {
    return "Hello, " + name + "!";
}

// Call the function and log the result
let result = greet("World");
console.log(result);
